import * as PaginatorTypes from '../../types/pagination.types';
import { getPaginatedResult, getPagination } from './get-paginated-result';
import { paginator } from './paginator';
import { searchPaginator } from './search-paginator';

export {
  getPaginatedResult,
  getPagination,
  paginator,
  PaginatorTypes,
  searchPaginator,
};
